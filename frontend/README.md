# Frontend

Static HTML, CSS, and JavaScript frontend.

## Running

1. Start the backend
2. Open `index.html` in a browser (or server, e.g. with Live Server)

## Backend Dependency

Expects the backend API at: http://localhost:5000/api/mood
